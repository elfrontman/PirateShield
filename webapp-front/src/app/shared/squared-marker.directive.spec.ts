import { SquaredMarkerDirective } from './squared-marker.directive';

describe('SquaredMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new SquaredMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
